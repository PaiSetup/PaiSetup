diff --git a/blocks.def.h b/blocks.def.h
index 9c22d68..fc8ae7a 100644
--- a/blocks.def.h
+++ b/blocks.def.h
@@ -9,3 +9,15 @@ static const Block blocks[] = {
 //sets delimeter between status commands. NULL character ('\0') means no delimeter.
 static char delim[] = " | ";
 static unsigned int delimLen = 5;
+
+// Button roles definition
+// left click  - 1
+// wheel click - 2
+// right click - 3
+// scroll up   - 4
+// scroll down - 5
+static const char* buttonAction = "1";
+static const char* buttonTerminate = "2";
+static const char* buttonInfo = "3";
+static const char* buttonScrollUp = "4";
+static const char* buttonScrollDown = "5";
diff --git a/dwmblocks.c b/dwmblocks.c
index 13ec863..927354e 100644
--- a/dwmblocks.c
+++ b/dwmblocks.c
@@ -68,6 +68,11 @@ void getcmd(const Block *block, char *output)
 
 	strcpy(output, block->icon);
 	FILE *cmdf;
+	setenv("BUTTON_ACTION", buttonAction, 1);
+	setenv("BUTTON_TERMINATE", buttonTerminate, 1);
+	setenv("BUTTON_INFO", buttonInfo, 1);
+	setenv("BUTTON_SCROLL_UP", buttonScrollUp, 1);
+	setenv("BUTTON_SCROLL_DOWN", buttonScrollDown, 1);
 	if (*button)
 	{
 		setenv("BUTTON", button, 1);
@@ -79,6 +84,11 @@ void getcmd(const Block *block, char *output)
 	{
 		cmdf = popen(block->command, "r");
 	}
+	unsetenv("BUTTON_ACTION");
+	unsetenv("BUTTON_TERMINATE");
+	unsetenv("BUTTON_INFO");
+	unsetenv("BUTTON_SCROLL_UP");
+	unsetenv("BUTTON_SCROLL_DOWN");
 	if (!cmdf)
 		return;
 	int i = strlen(block->icon);
